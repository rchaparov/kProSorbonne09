# Инструкция для Review Process (Architect-Orchestrator)
# Проект: TeamSpace | Команда: Сорбона 09

---

## КОГДА ПРОСИТЬ АГЕНТА ПЕРЕЧИТАТЬ ИНСТРУКЦИЮ

Только если:
- Новая сессия агента (новый чат)
- Смена модели агента
- В `CODING_AGENT.md` были внесены изменения

Во всех остальных случаях — сразу выдавать Task Card.

---

## ТРИГГЕР

Cursor Agent написал **"проверь"**.

---

## ШАГ 1 — ЧЕКЛИСТ РЕВЬЮ КОДА

Читать только файлы из секции "Затрагиваемые файлы" Task Card.

```
[ ] Отчёт workflow/done/task_NNN_done.md создан и заполнен
[ ] Все Acceptance Criteria из Task Card выполнены
[ ] Код соответствует стандартам (именование, типизация, структура)
[ ] Нет изменений сверх скоупа задачи
[ ] Git commit в правильном формате type(scope): description
[ ] Нет TODO/FIXME/хардкода без причины
[ ] Нет .env с реальными секретами
[ ] Нет эмодзи в шаблонах (только SVG иконки)
[ ] push ожидает "ПРИНЯТО"
```

---

## МАНИФЕСТ ПРОВЕРКИ КОДА (обязателен при каждом ревью)

1. Синтаксис: `python -c "import ast; ast.parse(open('file.py').read())"`
2. Импорты: каждый используемый класс импортирован в файле
3. Дубликаты: `grep "^def \|^class " file.py | sort | uniq -c | awk '$1 > 1'`
4. FastAPI Depends: `Depends()` получает функцию, не результат вызова
5. Шаблоны: все переменные, переданные в `TemplateResponse`, используются в HTML
6. Права доступа: каждый роут проверяет роль через зависимость
7. Файловый upload: проверка размера ДО записи в БД

---

## ШАГ 2 — ОБНОВЛЕНИЕ СТАТУСОВ (после принятия)

```
[ ] .workflow/done/task_NNN_done.md — присутствует
[ ] .workflow/PROJECT_STATUS.md — обновить таблицу задач
[ ] .workflow/CHANGELOG.md — добавить запись
```

---

## РЕЗУЛЬТАТ: ПРИНЯТО

```
ПРИНЯТО — Task #NNN

Обновляю PROJECT_STATUS.md и CHANGELOG.md.
Следующая задача: #NNN+1 — [название]. Выдаём?
```

---

## РЕЗУЛЬТАТ: ВОЗВРАЩЕНО

```
ВОЗВРАЩЕНО — Task #NNN

Причины:
1. [что не так и где]

Требуемые правки:
- [ ] [точный фикс]
```

---

## HOTFIX ПРОЦЕСС

1. Приостановить текущую таску (вернуть в backlog)
2. Создать `task_NNN_hotfix.md` в `in-progress/`
3. Выдать fix-таску с меткой [HOTFIX]
4. После принятия — вернуться к приостановленной таске
